from django.conf.urls import url
# from web.views import TodoListView, TodoDetailView

# urlpatterns = [
#     url(r'^$', TodoListView.as_view(), name="todo_list"),
#     url(r'^/(?P<pk>\d+)/(?P<slug>[-\w]*)/$', TodoDetailView.as_view(), name="todo_detail"),
# ]
